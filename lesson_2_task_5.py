"""
Создать вручную список, содержащий цены на товары (10–20 товаров), например:
[57.8, 46.51, 97, ...]

Вывести на экран эти цены через запятую в одну строку,
    цена должна отображаться в виде <r> руб <kk> коп (например «5 руб 04 коп»).
    Подумать, как из цены получить рубли и копейки, как добавить нули, если,
    например, получилось 7 копеек или 0 копеек (должно быть 07 коп или 00 коп).
Вывести цены, отсортированные по возрастанию, новый список не создавать
    (доказать, что объект списка после сортировки остался тот же).
Создать новый список, содержащий те же цены, но отсортированные по убыванию.
Вывести цены пяти самых дорогих товаров.
Сможете ли вывести цены этих товаров по возрастанию, написав минимум кода?

"""

my_arr = [57.8, 46.51, 44.04, 0, 97.0, 556, 44.3, 0.56, 22.09, 78, 66.3, 30, 99, 100]
final_str = ''

for i in range(len(my_arr)):
    number_to_format = '{:.2f}'.format(my_arr[i])
    point_split = number_to_format.split('.')

    if i == len(my_arr) - 1:
        final_str += f'{point_split[0]} руб {point_split[1]} коп'
    else:
        final_str += f'{point_split[0]} руб {point_split[1]} коп, '

print(final_str + '\n')

print(f'Объект списка до сортировки: {id(my_arr)}')
print(f'Сортировка по возрастанию: {sorted(my_arr)}')
print(f'Объект списка после сортировки: {id(my_arr)}' + '\n')

dec_sorted_arr = sorted(my_arr, reverse=True)
print(f'Список сортированный по убыванию: {dec_sorted_arr}')
print(f'Первые 5 самых дорогих: {dec_sorted_arr[:5]}')
print(f'Первые 5 самых дорогих по возрастанию (минимум кода?): {sorted(dec_sorted_arr[:5])}')
